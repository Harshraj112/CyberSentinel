services:
  - type: web
    name: cybersentinel-ml
    env: docker
    region: oregon
    plan: free
    healthCheckPath: /health
    envVars:
      - key: MONGODB_URL_KEY
        sync: false   # Set this manually in Render dashboard
